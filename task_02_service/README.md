# Task 2: FastAPI + Streamlit (CRUD)

## Запуск

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

- Терминал 1: `cd backend && python -m uvicorn main:app --reload`
- Терминал 2: `cd frontend && python -m streamlit run app.py`
